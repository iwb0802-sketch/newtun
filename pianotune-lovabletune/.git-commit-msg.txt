feat: 시험용2 - 인접건반 B 예측 고도화 (브레이크포인트 감지 + 신뢰도가중 + 이상탐지)

1. 브레이크포인트 처리 (scaleLearning.ts predictB)
   - 단순 역거리가중평균 대신 로그(B)공간에서 가중 선형회귀
   - 인접 측정치 사이 ln(B) 점프가 큰 지점(저음부 권선/단현 전환 등)을 브레이크포인트로
     감지해서, 타겟 건반과 같은 구간의 이웃만 회귀에 사용

2. 신뢰도 가중치
   - refineByPartialFitV2가 confidence(0~1, 적합오차+사용배음수 기반)와 nPartials를
     함께 반환하도록 확장 (TWMResult, CompositeResult)
   - predictB의 가중치 = 거리가중 × 신뢰도 -> 애매하게 측정된 이웃이 결과를 덜 오염시킴

3. 이상 감지(anomaly detection)
   - 확정 시점에 인접음 예측 B vs 실측 B 비교, 40% 이상 벗어나면
     "예상과 다른 배음 패턴 감지" 토스트 + 카운트 배지 표시 (참고용, 차단 아님)

4. 세션 저장 스키마 확장
   - KeyMeasurement: inharmonicityB, inharmonicityConfidence, nPartialsUsed 추가
   - recordMeasurement가 세 값을 모두 선택적으로 저장 -> 추후 피아노별 전체
     스케일링 커브 리포트 기능으로 확장 가능한 기반 마련

시험용/복합/실험실/자동 탭 영향 없음 (모두 선택적 파라미터로 하위호환 유지)
